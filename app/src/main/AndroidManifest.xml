<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.klz.reacnathuris">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".activities.Login.LoginActivity"
            android:label="@string/login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Login.RegisterActivity"
            android:label="@string/registrar" />
        <activity
            android:name=".activities.menus.MenuGeralActivity"
            android:label="@string/menu" />
        <activity
            android:name=".activities.menus.MenuCadastrosActivity"
            android:label="@string/cadastros" />
        <activity
            android:name=".activities.cadastros.CadPacientesActivity"
            android:label="@string/pacientes" />
        <activity
            android:name=".activities.cadastros.CadComplementoPacientesActivity"
            android:label="@string/pacientes" />
        <activity
            android:name=".activities.cadastros.CadUnidadesActivity"
            android:label="@string/unidades" />
        <activity
            android:name=".activities.cadastros.CadProtocolosActivity"
            android:label="@string/protocolos" />
        <activity
            android:name=".activities.Calendar.CalendarioActivity"
            android:label="@string/agenda" />
        <activity
            android:name=".activities.Calendar.AgendarAtendimentoActivity"
            android:label="@string/agenda" />
        <activity
            android:name=".activities.menus.MenuAnamneseActivity"
            android:label="@string/paciente_diagnosticado" />
        <activity
            android:name=".activities.perguntas.PerguntasDepressaoActivity"
            android:label="@string/anamnese_depressao" />
        <activity
            android:name=".activities.perguntas.PerguntasPanicoActivity"
            android:label="@string/anamnese_panico" />
        <activity
            android:name=".activities.perguntas.PerguntasAutismoActivity"
            android:label="@string/anamnese_autismo" />
        <activity
            android:name=".activities.perguntas.PerguntasDorActivity"
            android:label="@string/anamnese_dor" />
        <activity
            android:name=".activities.perguntas.PerguntasOutrosActivity"
            android:label="@string/anamnese_outros" />
        <activity
            android:name=".activities.resultado.ReceitasActivity"
            android:label="@string/incluir_receitas"/>
        <activity
            android:name=".activities.resultado.DespesasActivity"
            android:label="@string/incluir_despesas"/>
        <activity
            android:name=".activities.resultado.ResultadoRateioActivity"
            android:label="@string/resultado"/>
        <activity
            android:name=".activities.menus.MenuResultadosActivity"
            android:label="Menu financeiro"></activity>
    </application>

</manifest>